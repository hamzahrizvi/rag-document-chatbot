# rag-document-chatbot
Local AI chatbot built with Streamlit, LangChain, FAISS, and Ollama. Upload documents, retrieve relevant content with vector search, and generate context-aware answers using a Retrieval-Augmented Generation (RAG) pipeline. Runs locally with no API cost.
